#section
  #page_wrapper
    #content
      div class="post"
        ul
        li = @post.title
        li = @post.post_body
        -@post.pictures.each do |pic|
          li = image_tag pic.image.url(:medium)
        = "posted #{time_ago_in_words(@post.created_at)}"

      #comments
        h2
          = @post.comments.count
          |  Comments
        = render @post.comments
        |  Add a Comment:
        - if current_user && user_signed_in?
          = render "comments/form"
